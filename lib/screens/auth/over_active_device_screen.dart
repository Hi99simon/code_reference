import 'package:flutter/material.dart';

class OverActiveDeviceScreen extends StatefulWidget {
  const OverActiveDeviceScreen({Key? key}) : super(key: key);

  @override
  _OverActiveDeviceScreenState createState() => _OverActiveDeviceScreenState();
}

class _OverActiveDeviceScreenState extends State<OverActiveDeviceScreen> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
